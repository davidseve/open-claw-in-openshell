apiVersion: v2
name: keycloak
description: "Keycloak OIDC issuer for the OpenShell CLI/gRPC gateway auth path (see ADR-0016 — browser UI auth uses OpenShift-native OAuth via oauth-proxy instead, not this chart)"
type: application
version: 0.1.0
appVersion: "24.0"
